// Copyright © 2025 Raj Ramamurthy.

import SwiftUI

struct RouteTimeline: View {
  @Binding var document: FitExploreDocument
  var body: some View {
    // TODO: implement. This is intended to be similar to Final Cut's timeline
    // view, which can help filter down the selection, but it is also a chart
    // with heart rate, cadence, and power visible on the y axis. (These all
    // have a similar range so can possibly be charted together)
  }
}
